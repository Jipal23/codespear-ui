import { Container } from "react-bootstrap";

export const Second = props => {
    return (
        <Container fluid={true}>
            <div>
                Seconds page
            </div>
        </Container>
    );
}