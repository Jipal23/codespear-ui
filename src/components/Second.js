import { Container } from "react-bootstrap";

export const Second = props => {
    return (
        <Container fluid>
            <div>
                Seconds page
            </div>
        </Container>
    );
}